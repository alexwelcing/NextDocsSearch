# Timeline Convergence Article System Environment Variables
# Copy this to .env.local and fill in your values

# REQUIRED - OpenAI API Key for article generation
OPENAI_KEY=sk-your-openai-api-key-here

# REQUIRED - API Key for article generation endpoint
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ARTICLE_GENERATION_API_KEY=your-secure-api-key-here

# REQUIRED - Secret for Vercel Cron authentication
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CRON_SECRET=your-cron-secret-here

# OPTIONAL - Your site URL for internal API calls
NEXT_PUBLIC_SITE_URL=https://yourdomain.com

# OPTIONAL - Existing environment variables (keep these)
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
SUPABASE_SERVICE_ROLE_KEY=your-supabase-key
